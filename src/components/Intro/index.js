import React from 'react';
import { intro } from './intro.module.scss';

const Intro = () => {
  return (
    <div className={intro}>
      <h1>not found.</h1>
    </div>
  );
};

export default Intro;
